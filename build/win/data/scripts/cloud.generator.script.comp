#version 450

layout (local_size_x = 1, local_size_y = 1, local_size_z = 1) in;
layout (rgba16f, binding = 0) uniform image3D uTexture;

float noise(vec2 x) {
    return dot(x, x);
}

void main() {
    ivec3 pixelCoord = ivec3(gl_GlobalInvocationID.xyz);

}